<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Proyectos</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        :root {
            --primary-color: #1E40AF;
            --secondary-color: #2563EB;
            --accent-color: #3B82F6;
            --text-color: #1F2937;
            --background-color: #F3F4F6;
        }

        body {
            font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            background-color: var(--background-color);
        }

        .dashboard-card {
            background-color: white;
            border-radius: 0.75rem;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            transition: transform 0.3s ease;
        }

            .dashboard-card:hover {
                transform: translateY(-5px);
            }

        .stat-value {
            color: var(--primary-color);
            font-weight: 700;
            font-size: 1.75rem;
        }

        .dashboard-btn {
            background-color: var(--secondary-color);
            color: white;
            transition: all 0.3s ease;
        }

            .dashboard-btn:hover {
                background-color: var(--primary-color);
                transform: translateY(-2px);
                box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            }
    </style>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <header class="text-center mb-12">
            <h1 class="text-4xl font-bold text-gray-900 mb-4">Tablero de Gestión de Proyectos</h1>
            <p class="text-xl text-gray-600">Administra tus proyectos de manera eficiente y profesional</p>
        </header>

        <div class="grid md:grid-cols-3 gap-6 mb-12">
            <div class="dashboard-card p-6 text-center">
                <h3 class="text-lg font-semibold text-gray-700 mb-4">Total Proyectos</h3>
                <div class="stat-value">@Model.TotalProyectos</div>
                <p class="text-green-600 flex justify-center items-center">
                    <svg class="w-5 h-5 mr-1" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.707l-3-3a1 1 0 00-1.414 1.414L10.586 9H7a1 1 0 100 2h3.586l-1.293 1.293a1 1 0 101.414 1.414l3-3a1 1 0 000-1.414z" clip-rule="evenodd" />
                    </svg>
                    +0
                </p>
            </div>

            <div class="dashboard-card p-6 text-center">
                <h3 class="text-lg font-semibold text-gray-700 mb-4">Total Participantes</h3>
                <div class="stat-value">@Model.TotalParticipantes</div>
                <p class="text-green-600 flex justify-center items-center">
                    <svg class="w-5 h-5 mr-1" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.707l-3-3a1 1 0 00-1.414 1.414L10.586 9H7a1 1 0 100 2h3.586l-1.293 1.293a1 1 0 101.414 1.414l3-3a1 1 0 000-1.414z" clip-rule="evenodd" />
                    </svg>
                    +0
                </p>
            </div>

            <div class="dashboard-card p-6 text-center">
                <h3 class="text-lg font-semibold text-gray-700 mb-4">Total Tareas</h3>
                <div class="stat-value">@Model.TotalTareas</div>
                <p class="text-red-600 flex justify-center items-center">
                    <svg class="w-5 h-5 mr-1" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.707l-3-3a1 1 0 00-1.414 1.414L10.586 9H7a1 1 0 100 2h3.586l-1.293 1.293a1 1 0 101.414 1.414l3-3a1 1 0 000-1.414z" clip-rule="evenodd" />
                    </svg>
                    -0
                </p>
            </div>
        </div>

        <div class="grid md:grid-cols-3 gap-6">
            <div class="dashboard-card p-6 text-center">
                <h2 class="text-2xl font-bold text-gray-800 mb-4">Proyectos</h2>
                <p class="text-gray-600 mb-6">Administra y supervisa tus proyectos con un vistazo</p>
                <a href="@Url.Action("Index", "Proyectoes")" class="dashboard-btn px-6 py-3 rounded-lg inline-block">
                    Ver Proyectos
                </a>
            </div>

            <div class="dashboard-card p-6 text-center">
                <h2 class="text-2xl font-bold text-gray-800 mb-4">Tareas</h2>
                <p class="text-gray-600 mb-6">Crea, asigna y prioriza tus tareas</p>
                <a href="@Url.Action("Index", "Tareas")" class="dashboard-btn px-6 py-3 rounded-lg inline-block">
                    Ver Tareas
                </a>
            </div>

            <div class="dashboard-card p-6 text-center">
                <h2 class="text-2xl font-bold text-gray-800 mb-4">Participantes</h2>
                <p class="text-gray-600 mb-6">Gestiona los miembros de tu equipo</p>
                <a href="@Url.Action("Index", "Participantes")" class="dashboard-btn px-6 py-3 rounded-lg inline-block">
                    Ver Participantes
                </a>
            </div>
        </div>

        <!-- Mover los gráficos aquí, después de las tarjetas -->
        <div class="grid md:grid-cols-2 gap-8 mb-12">
            <div class="dashboard-card p-6 flex flex-col">
                <h3 class="text-xl font-semibold text-gray-800 mb-6 text-center">Tareas Completadas</h3>
                <div class="flex-grow relative">
                    <canvas id="tareasCompletadasChart" class="absolute inset-0 w-full h-full"></canvas>
                </div>
            </div>

            <div class="dashboard-card p-6 flex flex-col">
                <h3 class="text-xl font-semibold text-gray-800 mb-6 text-center">Tareas Pendientes</h3>
                <div class="flex-grow relative">
                    <canvas id="tareasPendientesChart" class="absolute inset-0 w-full h-full"></canvas>
                </div>
            </div>
        </div>

    </div>
    <br>
    <script>
        var ctx1 = document.getElementById('tareasCompletadasChart').getContext('2d');
        var ctx2 = document.getElementById('tareasPendientesChart').getContext('2d');

        var tareasCompletadasChart = new Chart(ctx1, {
            type: 'pie',
            data: {
                labels: ['Completadas', 'Pendientes'],
                datasets: [{
                    data: [@Model.TareasCompletadas, @Model.TareasPendientes],
                    backgroundColor: ['#10B981', '#EF4444'],
                    borderWidth: 1
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: true,
                layout: {
                    padding: 20
                },
                plugins: {
                    legend: {
                        position: 'bottom',
                        labels: {
                            font: {
                                size: 14,
                                family: "'Inter', sans-serif"
                            }
                        }
                    },
                    tooltip: {
                        callbacks: {
                            label: function (tooltipItem) {
                                return tooltipItem.label + ': ' + tooltipItem.raw;
                            }
                        }
                    }
                }
            }
        });

        var tareasPendientesChart = new Chart(ctx2, {
            type: 'bar',
            data: {
                labels: ['Tareas'],
                datasets: [{
                    label: 'Tareas Pendientes',
                    data: [@Model.TareasPendientes],
                    backgroundColor: '#EF4444',
                    borderWidth: 1
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: true,
                layout: {
                    padding: 20
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        ticks: {
                            precision: 0,
                            font: {
                                size: 14,
                                family: "'Inter', sans-serif"
                            }
                        }
                    }
                },
                plugins: {
                    tooltip: {
                        callbacks: {
                            label: function (tooltipItem) {
                                return 'Tareas Pendientes: ' + tooltipItem.raw;
                            }
                        }
                    }
                }
            }
        });
    </script>
    
    

    
</body>
</html>
